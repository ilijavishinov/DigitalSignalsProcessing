The shell scripts were executed in the cygwin wfdb directory, so the paths will not match with the paths in the project files.
However, they can be easily modified. They are showcased here so that the process of parsing data directly from physionet can be seen.


The shell scripts were executed with the following commands:
bash rdann_afdb_cudb_mitdb.sh
bash rdsamp_afdb_cudb_mitdb.sh
